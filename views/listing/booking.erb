<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  <link rel="stylesheet" href="<%= url("style.css") %>" type="text/css">
  <title>Listings | MakersBnB</title>
</head>
<body>
<div class="parallax">
  <nav class="navbar navbar-light" style="background-color: rgba(255, 255, 255, 0.3);">
    <img src="../images/bed-and-breakfast.svg" class="change-my-color-size">
    <a class="navbar-brand logo" href="../" style="color: white">MakersBnB</a>
    <ul class="navbar-nav ml-auto">
      <li class="navbar-item">
        <a class="navbar-link nav-right" href="../about-us">About</a> 
        <a class="navbar-link nav-right" href="../user/login">Login</a>
      </li>
    </ul>
  </nav>
   <div id="wrapper" class="container-fluid p-5">
      <div class="d-flex flex-column justify-content-center">
        <h4 class="pt-5">Listing: <%= @listing.name %></h4><br>
        <p>Description:<br><%= @listing.description %></p><br>
        <p>£<%= @listing.price %> - Per Night</p>
      </div>
      <div class="container  text-center">
      <form action="/listing/<% @listing.listing_id %>" method="get">
      <label for="trip-date" class="mt-3 p-2">Select A Date:</label>
      <input type="date" id="dt" />
      <form action="/booking" method="post">
        <input type="submit"  class="btn btn-primary mx-auto submit-btn" value="Request to book">
        <span id="datetime"></span>
      </form>
        <div class="row justify-content-center">
          <p class="warning fade-in">
            <%= flash[:booking_warning] %>
          </p>
      </div>
    </div>
  </div>
</body>
<script>
  document.getElementById('dt').min = new Date(new Date().getTime() - new Date().getTimezoneOffset() * 60000).toISOString().split("T")[0];
  document.getElementById('dt').value = new Date(new Date().getTime() - new Date().getTimezoneOffset() * 60000).toISOString().split("T")[0];
</script>
</html>